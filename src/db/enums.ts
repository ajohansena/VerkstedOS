/**
 * Centralized PostgreSQL enum declarations.
 *
 * All `pgEnum(...)` definitions live here so they have a single source of
 * truth and can be referenced from any schema file
 * (see docs/03-data-model.md § Types). Populated from Sprint 2 onward.
 */

export {};
