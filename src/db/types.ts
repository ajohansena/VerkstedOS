/**
 * Inferred row types for the schema (`InferSelectModel` / `InferInsertModel`).
 *
 * Exposed from here so application and presentation layers never import Drizzle
 * table objects directly. Populated alongside the schemas from Sprint 2 onward.
 */

export {};
