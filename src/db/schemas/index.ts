/**
 * Drizzle schema barrel.
 *
 * One file per table under `src/db/schemas/`, re-exported here. This is the
 * single entry point Drizzle Kit reads (see drizzle.config.ts).
 *
 * Sprint 1 ships no business tables — the foundation tables (organizations,
 * workshops, workshop_departments, users, memberships) arrive in Sprint 2.
 */

export {};
