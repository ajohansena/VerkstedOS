/**
 * Centralized Drizzle `relations(...)` declarations.
 *
 * Keeping relations in one place (rather than scattered across schema files)
 * keeps cross-table wiring readable. Populated from Sprint 2 onward.
 */

export {};
